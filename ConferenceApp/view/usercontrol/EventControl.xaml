<UserControl x:Class="ConferenceApp.view.usercontrol.EventControl"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:hm="http://schemas.hpod9.com/haley/mvvm"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="600">
    <StackPanel>
        <ComboBox Grid.Row="4" Grid.Column="1"
                  Name="ComboBox"
                  Margin="10,5"
                  IsTextSearchEnabled="False"
                  IsEditable="False"
                  FontSize="18"
                  materialDesign:HintAssist.Hint="Select event"
                  IsSynchronizedWithCurrentItem="True"
                  SelectionChanged="ComboBox_OnSelectionChanged"
                  ItemsSource="{Binding conferenceBindingList}"
                  SelectedItem="{Binding Path=SelectedConference, UpdateSourceTrigger=PropertyChanged}">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Name}" />
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>

        <Button Name="Create_Button" Click="Create_Button_Click" Margin="10,5" MinWidth="80">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <materialDesign:PackIcon Kind="Add" Width="19" Height="19" Foreground="White"
                                         VerticalAlignment="Stretch" Margin="0,0,2,0" />
                <TextBlock Text="{hm:Lang create_event}" />
            </StackPanel>
        </Button>

        <DataGrid Name="SessionDataGrid"
                  AutoGenerateColumns="False"
                  ColumnWidth="*"
                  VerticalScrollBarVisibility="Auto"
                  IsReadOnly="True">
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Copy" Command="ApplicationCommands.Copy" />
                    <MenuItem Header="Edit" Click="Edit_MenuItem_OnClick" />
                    <MenuItem Header="Delete" Click="Delete_MenuItem_OnClick" />
                </ContextMenu>
            </DataGrid.ContextMenu>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Session Name" Binding="{Binding Name,Mode=TwoWay}" />
                <DataGridTextColumn Header="Description" Binding="{Binding Description, Mode=TwoWay}" />
                <DataGridTextColumn Header="Start date"
                                    Binding="{Binding StartDate, StringFormat=d}" />
                <DataGridTextColumn Header="End date"
                                    Binding="{Binding EndDate, StringFormat=d}" />
            </DataGrid.Columns>
        </DataGrid>
    </StackPanel>
</UserControl>